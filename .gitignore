# dependencies — never commit, PMs don't need them
mcp-server/node_modules/

# API keys — never commit
.env
.env.local

# build artifacts — dist/ IS committed so PMs skip npm install
# do NOT add mcp-server/dist/ here

docs/user-guide/admin-guide/node_modules/
docs/user-guide/admin-guide/dist/
